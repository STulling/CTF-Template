name: Online challenge
category: Web
is_http: true
ctfd:
  type: dynamic_iac # Ignore
  slug: online_challenge
  tags:
    - easy
  bundle:
    include:
      - online_challenge/
      - docker-compose.yml
  flags:
    - content: FLAG{Nice_Job_Finding_The_Flag}
      type: static # Ignore
  challenge:
    description: |
      I hope you won't find the flag toooo easily 
    attribution: Testuser
    state: visible # Ignore
    initial: 500 # Ignore
    minimum: 100 # Ignore
    decay: 50 # Ignore
  dynamic_iac: # Ignore
    timeout: 1800 # Ignore
    mana_cost: 0 # Ignore
    min: 10  # Ignore
    max: 100 # Ignore
    additional:  # Ignore
      env.FLAG: "FLAG{Nice_Job_Finding_The_Flag}" # Use this if you want to inject the flag as an environment variable into the container. You can then access it in your application code.